{
  "name": "rant-to-launch",
  "version": "0.0.0",
  "description": "Turn a founder rant into eight launch-ready assets with Replit-managed OpenAI, Postgres, and App Storage.",
  "license": "MIT",
  "author": {
    "name": "RajanthaR",
    "email": "rajantha.rc@gmail.com",
    "url": "https://github.com/RajanthaR/"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r run test",
    "test:watch": "pnpm -r --parallel --stream run test:watch"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
